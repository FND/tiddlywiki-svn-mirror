This overrides the default wiki serialiser to provide plugins in minified form.
It also adds a tiddler serialiser .minified

To install simply add the 2 pieces of python code to your root directory and edit your config file to add to system_plugins "pluginminifier" 
