## Script (Python) "login"
##bind container=container
##bind context=context
##bind namespace=
##bind script=script
##bind subpath=traverse_subpath
##parameters=root=None
##title=
##
# This script returns javascript containing ZW's status variables
# (username, loggedIn, etc).  If the user doesn't have the proper credentials,
# loggedIn will be false...

return container.html['status.html']()
