createPackage ccTiddly Plugin ReadMe

Status : experimental - not active

 Changes the create workspace macro behaviour so that users can automatically fetch the contents of packages (tiddlywiki files) and automatically insert them into the workspace they are creating. 
 
 
